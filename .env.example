# ローカル開発用（docker-compose.yml の設定と一致）
DATABASE_URL=postgresql://todo_user:todo_pass@localhost:5432/todo_dev

# better-auth の署名キー（任意の文字列でよい）
BETTER_AUTH_SECRET=change-me-local-secret

# Resend APIキー（メール送信が不要なら空でもOK）
RESEND_API_KEY=
